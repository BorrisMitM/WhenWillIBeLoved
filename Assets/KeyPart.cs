using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum KeyState { original, up, turned};
public class KeyPart : MonoBehaviour
{
    [SerializeField] private Sprite original;
}
